final.m is matlab code to perform image deblurring.

To set which blur you would liek to test (horzontal, vertical, out of focus), set the desired blur to true at the top of the code.

'motion' sets the amout of motion blur strength.

'focus_strength' sets the amout of out of focus blur strength.

Both are found at the top of the code.